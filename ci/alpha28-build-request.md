# Emipokemon 0.4.0-alpha.28

GitHub Actions build request after fixing patch application so the alpha.28 diff is applied inside the extracted source tree.

Target: Java 21, `./gradlew clean test build --no-daemon --stacktrace`.

Keep this PR draft until Cobbleverse confirms placeholders and block occlusion visually.
